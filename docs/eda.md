# EDA findings

Generated by `notebooks/01_data_audit.ipynb` and `notebooks/02_eda.ipynb`
from the exact cached snapshots. Numbers below describe the snapshots
recorded in `results/data_manifest.json`.

## Data audit

- Coverage: ADRO 2,541 rows, PTBA 2,541 rows, ITMG 2,542 rows
  (2016-01-04 to 2026-04-30); IDR=X 2,687 rows (includes weekends).
- Duplicate dates: none. Missing values: none. Non-finite prices: none.
- All prices strictly positive; `Adj Close` used for return construction.
- Calendar differences exist (FX trades on days equities do not), handled
  by backward as-of alignment with a one-observation lag.

## Target-series behavior

Daily adjusted-close log returns:

| Target | Mean | Std | Min | Max | Skew | Excess kurtosis |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| ADRO | 0.00122 | 0.0286 | -0.2829 | 0.1772 | -0.11 | 7.45 |
| PTBA | 0.00094 | 0.0253 | -0.1893 | 0.1924 | 0.21 | 7.80 |
| ITMG | 0.00114 | 0.0250 | -0.1052 | 0.1815 | 0.63 | 3.69 |

Returns sit near zero with heavy tails. Excess kurtosis runs 3.7 to 7.8
and a Jarque-Bera test rejects normality for all three, so the
heavy-tails label is earned, not assumed. Daily moves top 10% and ADRO
printed a -28% day. Volatility clusters over time; rolling 5/20-day
volatility features capture this. Near-zero means are why the zero-return
naive benchmark is competitive.

## External-variable behavior

USD/IDR daily log changes: mean near 0.00009, std near 0.0065. Equity
return volatility runs about 4x higher (ratios 3.9 to 4.4 across the three
targets). Coverage runs the full study period with no missing dates after
alignment.

## Relationships

Same-day correlations between target returns and external series are weak.
That is expected. The research question is whether *lagged* external
information helps, and correlation is descriptive, not causal. Lagged
correlation checks in the EDA notebook show no strong persistent lead-lag
pattern. That matches the later finding that external features do not
help consistently.

## Structural inspection

ADRO shows a visible regime shift around the late-2024 ADRO/AADI
restructuring. The EDA notebook marks the event window. No formal break test
is run, so no break is claimed; the event is carried as a limitation and a
reason pre/post behavior may differ.
